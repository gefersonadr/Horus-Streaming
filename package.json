{
  "name": "horus-streaming",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "start": "vite preview --port $PORT --host"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}